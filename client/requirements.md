## Packages
recharts | For drawing sparkline charts on signal cards
framer-motion | For smooth entry animations and layout transitions
date-fns | For time formatting and relative time calculations

## Notes
- Auth is handled via Replit Auth integration (already set up)
- Signals have 'UP'/'DOWN' direction and strict timeframes (1m, 3m, 5m)
- Dashboard requires real-time feel, using polling interval of 5s for signals
